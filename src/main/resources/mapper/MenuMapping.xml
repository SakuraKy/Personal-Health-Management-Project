<?xml version="1.0" encoding="UTF-8"  ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.MenuMapping">
    <!-- 一对多关系  关系映射 -->
    <resultMap id="menuMap" type="com.atsakuraky.sportplay.bean.MainMenu">
        <!-- 前面一个对应数据库中的 后面一个对应实体 -->
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="path" property="path"/>
        <collection property="sList" ofType="com.atsakuraky.sportplay.bean.SubMenu">
            <id column="sid" property="id"/>
            <result column="stitle" property="title"/>
            <result column="spath" property="path"/>
        </collection>


    </resultMap>
    <select id="getMenus" resultMap="menuMap">
        SELECT mm.*, sm.id AS sid, sm.title AS stitle, sm.path AS spath
        FROM mainmenu mm, submenu sm
        WHERE mm.id = sm.mid;
    </select>

</mapper>
